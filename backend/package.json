{
  "name": "@careerbridge/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reserved backend service for CareerBridge. Currently a stub — no business logic yet.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
