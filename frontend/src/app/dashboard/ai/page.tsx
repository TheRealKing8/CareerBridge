import { PlaceholderPanel } from "@/components/dashboard/PlaceholderPanel";

export default function Page() {
  return (
    <PlaceholderPanel
      title="CareerBridge AI"
      description="The floating chat widget in the bottom-right already has AI access — click it for career Q&A. The dedicated AI page will add CV analysis and cover-letter generation in Phase 2e."
    />
  );
}